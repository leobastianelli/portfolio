import type { ProjectMeta } from "@/lib/content/types";

const meta: ProjectMeta = {
  slug: "scout",
  year: "2025",
  status: "building",
  stack: ["agentes", "automatización"],
  links: [],
  featured: false,
};

export default meta;
